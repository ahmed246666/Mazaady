import CategoryForm from "../_components/CategoryForm/CategoryForm";

const Form: React.FC = async () => {
  return (
    <div className="sm:p-8 py-3">
      <CategoryForm />
    </div>
  );
};

export default Form;
